<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ylm.community.mapper.PostReplyInfoMapper">

    <sql id="Base_Column_List">
        id,uid, ,is_delete,create_time,update_time
    </sql>

    <sql id="Insert_Column_List">
        uid,
    </sql>

    <sql id="Table">
        ``
    </sql>

    <resultMap id="BaseResultMap" type="com.ylm.community.model.entity.PostReplyInfoEntity">
        <id column="id" property="id"/>
        <result column="uid" property="uid"/>

        <result column="is_delete" property="isDelete"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="get" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        <include refid="Table"/>
        WHERE
        is_delete = 0
        AND username = #{username}
        ORDER BY id desc;
    </select>

    <insert id="add" parameterType="com.ylm.community.model.entity.PostReplyInfoEntity">
        INSERT INTO
        <include refid="Table"/>
        (<include refid="Insert_Column_List"/>)
        VALUES
        ()
    </insert>

</mapper>